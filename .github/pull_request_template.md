## What is this?

<!-- One or two lines. If it adds a listing, say which app. -->

## Checklist

- [ ] `npm run validate` passes
- [ ] `npm run check` passes
- [ ] `npm run build` succeeds
- [ ] The listing is a single file in `data/apps/`, named after the app
- [ ] The app is live and usable without a login
- [ ] I built it, or the maker is fine with it being listed

## Notes for the reviewer

<!-- Anything that needs context: a brand new category, a sponsorship, a removal. -->
